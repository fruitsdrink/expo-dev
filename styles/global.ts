import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  page: {
    flex: 1,
    padding: 20,
  },
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});
